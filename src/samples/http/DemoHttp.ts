import {ViewProps} from 'react-native';
import {BaseComponent} from '../../aura/base/BaseComponent';

export default class DemoHttp extends BaseComponent<ViewProps, {}> {
  constructor(props: ViewProps) {
    super(props);
  }

}
